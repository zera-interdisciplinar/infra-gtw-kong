apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ms-inventory-swagger-qa
  namespace: qa
  annotations:
    kubernetes.io/ingress.class: "kong"
    konghq.com/plugins: "api-key-auth"
    konghq.com/strip-path: "true"
spec:
  ingressClassName: kong
  rules:
  - http:
      paths:
      - path: /qa/inventory/swagger-ui
        pathType: Prefix
        backend:
          service:
            name: ms-inventory
            port:
              number: 80
      - path: /qa/inventory/v3
        pathType: Prefix
        backend:
          service:
            name: ms-inventory
            port:
              number: 80
